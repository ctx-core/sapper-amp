{
	"name": "@ctx-core/sapper-amp",
	"version": "5.0.4",
	"description": "@ctx-core Svelte Sapper with Google amp support",
	"homepage": "https://github.com/ctx-core/sapper-amp#readme",
	"scripts": {
		"build": "npm run clean && npm run compile",
		"clean": "rm -f tsconfig.tsbuildinfo && rm -f dist/**/*.js && rm -f dist/**/*.d.ts && rm -f dist/**/*.map",
		"compile": "./node_modules/.bin/tsc -b .",
		"exec": "$@",
		"prepublishOnly": "npm run build"
	},
	"main": "./src/index.js",
	"module": "./src/index.js",
	"types": "./src/index.d.ts",
	"dependencies": {
		"svelte": "3.38.2"
	},
	"devDependencies": {
		"@ctx-core/ctx-core-package-tools": "workspace:^8.0.3",
		"typescript": "^4.3.3"
	},
	"publishConfig": {
		"access": "public",
		"cache": "~/.npm"
	}
}